#include <stdio.h>
#define MAX 100000000

int main(int argc, char const *argv[])
{
    int width;
    char str[MAX];
    int start[MAX]
    scanf("%d %s", width, str);
    char min = 'z';

    for(int i = 0; i<MAX; i++)
        
    
    return 0;
}
